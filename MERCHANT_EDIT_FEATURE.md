# 📝 توثيق: إضافة ميزة تعديل بيانات التاجر في صفحة الإشراف

## 🎯 ما تم إضافته؟

تم إضافة **نموذج تعديل شامل** لبيانات التاجر في صفحة `merchant_profile_admin.html` بنظام **Tabs** (علامات تبويب).

---

## 📂 الملفات المُعدّلة

### 1. `/var/www/bazarna/templates/supervisor/merchant_profile_admin.html`

- **الحالة**: ✅ تم تعديلها
- **التغييرات**:
  - إضافة نظام **Navigation Tabs** في الأعلى
    - **Tab 1: العرض** - عرض بيانات التاجر الحالية (الافتراضية)
    - **Tab 2: تعديل البيانات** - نموذج التعديل الكامل

---

## 🎨 الميزات المُضافة

### Tab 1️⃣: العرض (View Content)

عرض البيانات الحالية للتاجر:

- ✅ صورة المتجر/الشخصية
- ✅ اسم التاجر مع علامة التوثيق (إن وجدت)
- ✅ رقم الهاتف
- ✅ بيانات المتجر (الأنواع، الكمية، الأسعار)
- ✅ الوثائق (الرقم القومي، السجل الضريبي)
- ✅ روابط البطاقة الشخصية
- ✅ كروت الإحصائيات (الرصيد، المبيعات، المنتجات)
- ✅ سجل المعاملات المالية

### Tab 2️⃣: تعديل البيانات (Edit Content)

نموذج تفاعلي كامل يتضمن:

#### البيانات النصية:

- 📝 أنواع البضاعة (نص طويل)
- 📝 كمية البضاعة المحتملة
- 📝 متوسط الأسعار
- 📝 المقاسات المتاحة

#### الوثائق:

- 🆔 الرقم القومي (للعرض فقط - غير قابل للتعديل)
- 📋 رقم السجل الضريبي (قابل للتعديل)

#### خيارات التوثيق:

- ✓ checkbox لـ **موثّق** (يضيف علامة زرقاء)

#### تحديث الصور:

- 🖼️ صورة المتجر/الشخصية
- 📷 البطاقة الشخصية (الأمام)
- 📷 البطاقة الشخصية (الخلف)
- ✅ تنبيه أخضر يظهر إذا كانت الصورة موجودة حالياً

#### أزرار العمل:

- 💾 **حفظ التعديلات** - يرسل النموذج (POST)
- ❌ **إلغاء** - العودة للـ Tab الأول

---

## 🔄 كيفية عمل النظام

### المسار:

```
الإشراف → الملاحون → اختيار تاجر → صفحة الملف الشخصي
         ↓
      Tab 1: العرض (افتراضي)
         ↓
    يضغط على "تعديل البيانات"
         ↓
      Tab 2: النموذج
         ↓
    يعدل البيانات والصور
         ↓
    يضغط "حفظ التعديلات"
         ↓
      POST request إلى `merchant_profile_admin`
         ↓
    المعالجة في supervisor/views.py
         ↓
    رسالة نجاح وإعادة التوجيه
```

---

## 🔗 الروابط والـ URLs

### للوصول إلى الصفحة:

```
/super/merchants/profile/<merchant_id>/
```

**اسم الـ URL**: `super_merchant_profile`

### في القالب:

```django
{% url 'super_merchant_profile' merchant.id %}
```

---

## 🧪 البيانات المعالجة

الـ View الموجود في `supervisor/views.py` يعالج:

```python
POST parameters:
- goods_types: أنواع البضاعة
- goods_quantity: الكمية
- goods_average_price: متوسط الأسعار
- goods_sizes: المقاسات
- national_id: الرقم القومي (للقراءة فقط)
- tax_register: السجل الضريبي
- is_verified: حالة التوثيق (checkbox)
- shop_image: صورة المتجر (ملف)
- id_card_front: البطاقة الأمام (ملف)
- id_card_back: البطاقة الخلف (ملف)
```

---

## 📊 مثال على الاستخدام

### 1. المشرف يفتح الصفحة:

```
ينقر على: /super/merchants/all/ → اختيار تاجر
```

### 2. يرى البيانات الحالية:

```
✅ الصورة: موجودة
✅ الاسم: أحمد علي
✅ الرصيد: 2500 ج.م
✅ المنتجات: 12
```

### 3. ينقر على Tab "تعديل البيانات":

```
يرى النموذج الكامل مع البيانات المملوءة مسبقاً
```

### 4. يعدل البيانات:

```
- يغير أنواع البضاعة
- يرفع صورة جديدة
- يفعّل خيار التوثيق
```

### 5. ينقر "حفظ التعديلات":

```
✅ تم تحديث البيانات بنجاح
→ إعادة التوجيه للصفحة نفسها
→ ظهور رسالة النجاح
```

---

## 🎨 التصميم والمظهر

### الألوان:

- 🔵 **أزرق**: الأيقونات الرئيسية والعناوين الأساسية
- 🟢 **أخضر**: تنبيهات النجاح والصور الموجودة
- ⚫ **رمادي**: النصوص المساعدة والعناصر الثانوية

### الأيقونات:

- 👁️ `fa-eye` - للعرض
- ✏️ `fa-edit` - للتعديل
- 💾 `fa-save` - للحفظ
- ✔️ `fa-check-circle` - للتوثيق والتأكيد

### التخطيط:

- **Responsive**: يعمل على جميع الأحجام (موبايل، تابلت، ديسكتوب)
- **Bootstrap 5**: استخدام نظام الشبكة والمكونات
- **RTL**: مصمم للنصوص من اليمين لليسار

---

## ✅ الاختبار

### للتأكد من أن كل شيء يعمل:

1. **افتح الصفحة**:

   ```
   /super/merchants/profile/1/
   ```

2. **تحقق من Tab الأول**:
   - ✅ يظهر البيانات الحالية
   - ✅ يظهر الإحصائيات

3. **انقر على Tab الثاني**:
   - ✅ يظهر النموذج
   - ✅ البيانات مملوءة مسبقاً

4. **اختبر النموذج**:
   - ✅ أدخل بيانات جديدة
   - ✅ رفع ملفات صور
   - ✅ فعّل التوثيق

5. **انقر "حفظ"**:
   - ✅ يتم الحفظ بنجاح
   - ✅ رسالة نجاح تظهر
   - ✅ البيانات محدثة في Tab الأول

---

## 🔐 الأمان

### الحماية المستخدمة:

1. **@login_required**:
   - فقط المشرفون المسجلون يمكنهم الوصول

2. **is_supervisor() check**:
   - فقط المشرفون والمالكون يمكنهم الوصول

3. **CSRF Protection**:
   - كل نموذج يحتوي على `{% csrf_token %}`

4. **File Validation**:
   - قبول الصور فقط: `accept="image/*"`

5. **Readonly Fields**:
   - الرقم القومي لا يمكن تعديله (readonly)

---

## 📝 الملاحظات الهامة

### ✅ نقاط القوة:

- ✔️ واجهة بديهية وسهلة الاستخدام
- ✔️ نموذج شامل يغطي جميع بيانات التاجر
- ✔️ تنبيهات واضحة عن الصور الموجودة
- ✔️ تصميم متجاوب
- ✔️ معالجة آمنة للملفات

### ⚠️ الحدود:

- الرقم القومي لا يمكن تعديله (هذا مقصود للأمان)
- يتم حفظ الملفات الجديدة فقط، ولا يحتفظ بالقديمة تلقائياً

---

## 🚀 الخطوات التالية

### يمكن إضافة:

1. 📊 عرض معاينة للصور المرفوعة
2. 🔄 سجل بالتعديلات السابقة (audit trail)
3. ⏲️ آخر شخص عدّل البيانات والتاريخ
4. 📮 إرسال بريد للتاجر عند التعديلات الهامة
5. 🔔 إشعارات فورية للتجار بالتغييرات

---

## 📚 المراجع

### الملفات ذات الصلة:

- `/var/www/bazarna/supervisor/views.py` - معالج POST
- `/var/www/bazarna/supervisor/urls.py` - الروابط
- `/var/www/bazarna/store/models.py` - موديل MerchantProfile
- `/var/www/bazarna/templates/supervisor/base_supervisor.html` - القالب الأساسي

---

**تم الإنشاء**: 24 فبراير 2026
**الحالة**: ✅ جاهز للاستخدام
**الإصدار**: 1.0
